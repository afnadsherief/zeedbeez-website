# OG Images

Default Open Graph share image referenced by src/lib/constants.ts SITE_META.ogImage
must live here as `default.jpg` (1200x630, per Open Graph spec).

Phase 1 has no production brand photography yet — this placeholder must be
replaced before merge to production/before Phase 2 sharing features ship.
